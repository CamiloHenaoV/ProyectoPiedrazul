server:
  port: 8084

spring:
  application:
    name: scheduling-service
  datasource:
    url: jdbc:postgresql://localhost:5432/MsSchedulingDB
    username: postgres
    password: postgre
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin

user-management:
  url: http://localhost:8081
